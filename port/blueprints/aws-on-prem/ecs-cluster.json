{
  "identifier": "ecsCluster",
  "title": "ECS Cluster",
  "icon": "AWS",
  "schema": {
    "properties": {
      "name": {
        "type": "string",
        "title": "Cluster Name",
        "description": "The name of the ECS cluster"
      },
      "capacity_providers": {
        "type": "array",
        "title": "Capacity Providers",
        "description": "Capacity providers associated with this cluster",
        "items": {
          "type": "string"
        }
      },
      "cluster_settings": {
        "type": "array",
        "title": "Cluster Settings",
        "description": "Cluster settings from ECS (e.g. containerInsights)",
        "items": {
          "type": "object"
        }
      },
      "configuration": {
        "type": "object",
        "title": "Configuration",
        "description": "Execute command and logging configuration"
      },
      "default_capacity_provider_strategy": {
        "type": "array",
        "title": "Default Capacity Provider Strategy",
        "description": "Default capacity provider strategy for the cluster",
        "items": {
          "type": "object"
        }
      },
      "arn": {
        "type": "string",
        "title": "ARN",
        "description": "The Amazon Resource Name (ARN) of the ECS cluster"
      },
      "kind": {
        "type": "string",
        "title": "AWS Cloud Control API Kind",
        "description": "The kind of the AWS Cloud Control API resource"
      },
      "region": {
        "type": "string",
        "title": "Region",
        "description": "The AWS region where the cluster is located"
      },
      "tags": {
        "type": "array",
        "title": "Tags",
        "description": "Tags attached to the ECS cluster",
        "items": {
          "type": "object"
        }
      },
      "application_name": {
        "type": "string",
        "title": "Application Name",
        "description": "Value of the ApplicationName tag"
      },
      "environment_name": {
        "type": "string",
        "title": "Environment",
        "description": "Value of the Environment tag"
      }
    },
    "required": []
  },
  "mirrorProperties": {},
  "calculationProperties": {},
  "aggregationProperties": {},
  "relations": {
    "account": {
      "title": "AWS Account",
      "target": "awsAccount",
      "required": true,
      "many": false
    },
    "workload": {
      "title": "Workload",
      "target": "workload",
      "required": false,
      "many": true
    }
  }
}
